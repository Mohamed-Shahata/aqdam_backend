export enum UserRole {
  USER = "user",
  SUP_USER = "sup_user",
  ADMIN = "admin"
};

export enum ReactionType {
  BENEFITED = "benefited",
  NOT_BENEFITED = "not_benefited"
}

export enum GenderType {
  MALE = "male",
  FEMALE = "female"
}