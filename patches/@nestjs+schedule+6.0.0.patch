diff --git a/node_modules/@nestjs/schedule/dist/scheduler.orchestrator.js b/node_modules/@nestjs/schedule/dist/scheduler.orchestrator.js
index a2d8e5f..0d6d643 100644
--- a/node_modules/@nestjs/schedule/dist/scheduler.orchestrator.js
+++ b/node_modules/@nestjs/schedule/dist/scheduler.orchestrator.js
@@ -13,6 +13,8 @@ exports.SchedulerOrchestrator = void 0;
 const common_1 = require("@nestjs/common");
 const cron_1 = require("cron");
 const scheduler_registry_1 = require("./scheduler.registry");
+const { randomUUID } = require('crypto');
+
 let SchedulerOrchestrator = class SchedulerOrchestrator {
     constructor(schedulerRegistry) {
         this.schedulerRegistry = schedulerRegistry;
@@ -74,20 +76,20 @@ let SchedulerOrchestrator = class SchedulerOrchestrator {
     closeCronJobs() {
         Array.from(this.schedulerRegistry.getCronJobs().keys()).forEach((key) => this.schedulerRegistry.deleteCronJob(key));
     }
-    addTimeout(methodRef, timeout, name = crypto.randomUUID()) {
+    addTimeout(methodRef, timeout, name = randomUUID()) {
         this.timeouts[name] = {
             target: methodRef,
             timeout,
         };
     }
-    addInterval(methodRef, timeout, name = crypto.randomUUID()) {
+    addInterval(methodRef, timeout, name = randomUUID()) {
         this.intervals[name] = {
             target: methodRef,
             timeout,
         };
     }
     addCron(methodRef, options) {
-        const name = options.name || crypto.randomUUID();
+        const name = options.name || randomUUID();
         this.cronJobs[name] = {
             target: methodRef,
             options,
